# 掌上掼蛋移植验收方案

## 目标

Android 外壳只负责热点/局域网地址、二维码、前台服务和 WebView；实际牌局必须使用家庭版的牌型、牌力、出牌策略和 AI。

## 当前差异

- 电脑家庭版：Flask-Socket.IO + Python 游戏引擎。
- Android 参考外壳：内置 Node.js + HTTP/SSE。
- 因此不能直接替换静态网页；需要一个手机端协议适配层，并将牌局状态机与 AI 运行时移植到 Node.js。

## 移植顺序

1. 将家庭版牌型识别、比较、逢人配和合法出牌生成移植为纯 JavaScript 模块。
2. 将“减少出牌轮次、保留炸弹/同花顺、队友协作”的规则 AI 移植为 Node 模块。
3. 在 Node 服务中加入房间、座位、断线重连和快照广播。
4. 将家庭版 React 界面改为使用手机协议；保留出牌停留、二维牌面和 AI 牌查看调试开关。
5. 用同一组牌局回放样例对比 Python 与 Node 的动作、牌型和结算结果。

## APK 交付门槛

- App 标题显示“掌上掼蛋”。
- 页面标题和布局来自家庭版，而不是参考项目。
- 手机 A 开热点并创建房间，手机 B 加入后可完成一局。
- 至少两个 AI 位置自动补位。
- AI 动作日志能显示组合与牌力评分。
- 随机发牌通过统计测试，不出现非随机的炸弹偏差。
- 本地和 GitHub Actions 均通过后，才生成可交付 APK。
