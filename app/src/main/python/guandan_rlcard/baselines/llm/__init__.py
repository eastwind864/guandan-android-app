"""LLM baseline (requires the ``openai`` package and an API key).

Set credentials through environment variables (see ``client.py``); the
package never ships or reads hardcoded keys.

    from guandan_rlcard.baselines.llm.llm_agent import LLMAgent
"""
